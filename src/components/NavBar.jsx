function NavBar({ className = 'w-full h-12 tablet:w-40 tablet:h-full' }) {
    return (
        <nav className={`bg-slate-700 ${className}`}>
            NavBar
        </nav>
    );
}

export default NavBar;